const express = require('express');

// Initializations
const app = express();

// Settings

// Middlewares

// Global variables

// Routes

// Static files

// Start the server
app.listen(3000, () => {
  console.log(`Server on port 3000`);
});
